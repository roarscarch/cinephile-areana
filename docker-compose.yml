services:
  cinephile-areana:
    build: .
    container_name: cinephile-areana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      # optional: override the public TMDB key baked into extractor.js
      - TMDB_API_KEY=${TMDB_API_KEY:-}
